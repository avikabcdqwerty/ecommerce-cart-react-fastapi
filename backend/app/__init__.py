# This file initializes the FastAPI app package.
# It allows other modules to import from 'app' directly.

# No code is needed here for initialization,
# but you can add package-level imports if needed in the future.